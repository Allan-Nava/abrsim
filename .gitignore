# Build output
/abrsim
/dist/
*.exe

# Coverage artefacts, at the repository root only. Unanchored these would also
# match scripts/coverage.sh, which is source and has to be committed.
/cover.out
/coverage.*

# Editors and OS
.DS_Store
.idea/
.vscode/

# Local scratch: reports produced by running the tool in-tree
*.report.md
*.report.json

# graphify knowledge graph: regenerable from the sources.
graphify-out/

# Agent tooling state: hooks and permissions belong to a workstation, not to the
# project. The rules the agents follow are in AGENTS.md, which is committed.
.claude/settings.json
.codex/
